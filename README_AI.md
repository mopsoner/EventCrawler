## Hybrid AI-ready extraction
This initial version adds:
- rule-based extraction
- AI fallback extraction stubs
- page snapshots
- extraction confidence
- validation log
- learned pattern store

Set `EVENTCRAWLER_EXTRACTION_MODE=HYBRID` (default) to enable the hybrid flow.
